<h1><%= @party.name %></h1><br>
 
<p>Characters:</p>
<% @party.characters.each do |character| %>
  <div> <%= link_to character.name, party_character_path(@party, character.id) %></div>
<% end %> <br>

 
<%= link_to "New Character", new_party_character_path(@party) %> <br />
<%= link_to "Back to Party", campaign_party_path(@party.campaign_id, @party.id) %>